{
  "name": "ekmool",
  "private": true,
  "packageManager": "pnpm@11.5.1",
  "engines": {
    "node": ">=22.13"
  },
  "scripts": {
    "dev": "turbo dev --filter=web",
    "build": "turbo build --filter=web",
    "typecheck": "turbo typecheck",
    "lint": "turbo lint",
    "check:packages": "node scripts/check-shared-packages.mjs",
    "check:mobile": "node scripts/check-mobile.mjs",
    "web": "pnpm --filter web",
    "mobile": "pnpm --filter mobile"
  },
  "devDependencies": {
    "turbo": "2.10.9",
    "typescript": "^5"
  }
}
